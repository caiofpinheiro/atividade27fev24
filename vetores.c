#include <stdio.h>
#include <string.h>
#include <locale.h>
#include <stdlib.h>
#include <ctype.h>
#include <time.h>

int main(){
	int numeros[5];
	
	numeros[0] = 1;
	numeros[1] = 2;
	numeros[2] = 3;
	numeros[3] = 4;
	numeros[4] = 5;
	
	printf("Elemento 1: %i \n", numeros[0]);
	printf("Elemento 2: %i \n", numeros[1]);
	printf("Elemento 3: %i \n", numeros[2]);
	printf("Elemento 4: %i \n", numeros[3]);
	printf("Elemento 5: %i \n", numeros[4]);
	
}
